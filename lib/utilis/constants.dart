import 'package:flutter/cupertino.dart';

const primaryColor = Color.fromRGBO(0, 57, 103, 1);

const primaryHalfColor = Color.fromRGBO(0, 57, 103, 0.5);

const primaryThirdColor = Color.fromRGBO(0, 57, 103, 0.3);

const primaryDark = Color.fromRGBO(0, 34, 62, 1);

const primaryLightColor = Color.fromRGBO(0, 146, 229, 1);

const primaryLightLightColor = Color.fromRGBO(216, 239, 255, 1);

const primaryGradient = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Color.fromRGBO(11, 107, 188, 1), Color.fromRGBO(6, 68, 120, 1)]);

const secondaryGradient = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Color.fromRGBO(148, 148, 148, 1), Color.fromRGBO(81, 81, 81, 1)]);


const whiteColor = Color.fromRGBO(255, 255, 255, 1);

const redColor = Color.fromRGBO(255, 0, 0, 1);

const greenColor = Color.fromRGBO(0, 255, 0, 1);

const activeColor = Color.fromRGBO(0, 163, 255, 1);

const waitingDayColor = Color.fromRGBO(100, 157, 202, 1);

const waitingNightColor = Color.fromRGBO(60, 77, 90, 1);

const String apiEndPoint = 'https://spoyer.com/api/en/get.php';

const String apiLogin = 'ayna';

const String apiToken = '12784-OhJLY5mb3BSOx0O';

const int defaultLeagueId = 1328;